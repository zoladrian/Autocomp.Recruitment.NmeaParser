<UserControl x:Class="Autocomp.Nmea.PrismApp.Modules.ModuleName.Views.ParserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <TextBox Text="{Binding NmeaInput}" Width="300" Margin="5"/>
            <Button Content="Parse" Command="{Binding ParseCommand}" Width="100" Margin="5"/>
            <TextBlock Text="{Binding ParsedData}" FontSize="16" Margin="5" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
